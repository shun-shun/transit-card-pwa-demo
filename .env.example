# GAS Web App のデプロイURL（Apps Script > デプロイ > ウェブアプリ で発行されるURL）
VITE_GAS_ENDPOINT_URL=https://script.google.com/macros/s/xxxxxxxxxxxxxxxx/exec

# GAS側のスクリプトプロパティ SHARED_SECRET と同じ値を設定する
VITE_GAS_SHARED_SECRET=change-me

# 実際の値は .env.local にコピーして使う（.env.local は *.local として git 管理対象外）
